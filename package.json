{
  "name": "hack-the-valley-site",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check worker.js && node --check functions/_lib/event-platform.js && node --check functions/api/register.js && node --check functions/api/events/index.js && node --check functions/api/events/[slug].js && node --check functions/api/events/[slug]/signups/index.js && node --check functions/api/subscribe.js",
    "dev": "wrangler dev --port 8788",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
